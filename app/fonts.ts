import { Comfortaa, Redacted_Script } from "next/font/google";

export const comfortaa = Comfortaa({
  subsets: ["latin"],
  display: "swap",
});

export const redactedScript = Redacted_Script({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
});
